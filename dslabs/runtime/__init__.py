"""Runtime support for running nodes as separate processes/containers.

See `http_transport.py` for the FastAPI-based transport and `run_node.py` for
the executable entry point used by Docker Compose.
"""
